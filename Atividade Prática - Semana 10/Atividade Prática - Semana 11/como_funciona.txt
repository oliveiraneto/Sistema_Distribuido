Para testar a implementação, basta executar o servidor e, em seguida, executar o cliente em outra janela do terminal. 
O cliente solicitará que você digite uma mensagem para enviar ao servidor. 
Depois de enviar a mensagem, ela será exibida no console do servidor.